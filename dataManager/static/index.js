const submit = () => {
    var selected_data_source = document.getElementById("data_source")
    var selected_activity = document.getElementById("activity")

    alert(selected_data_source.value + ', ' + selected_activity.value)
}